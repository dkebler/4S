// HTML
// process all html first using post html then the current template engine.
module.exports = function(data) {
  Debug.L1('in style function');
  return new Promise(function (resolve, reject) {
  })
}
